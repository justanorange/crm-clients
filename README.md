# Тестовое задание. CRM: управление клиентами

Упрощенное CRM-приложение для управления клиентами с полным функционалом CRUD операций, фильтрацией и валидацией формы.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Разработка
```bash
npm run dev
```
Приложение откроется на `http://localhost:5173`

### Сборка для продакшна
```bash
npm run build
```

## 🏗️ Архитектурные решения

### 1. **Feature-Sliced Design (FSD)**
Проект организован по функциональным срезам:
- `src/app` — точка входа приложения
- `src/pages` — страницы приложения
- `src/features` — переиспользуемые функции (ClientForm)
- `src/entities` — бизнес-сущности (Client, Store)
- `src/shared` — общий код (компоненты, утилиты, stores)

### 2. **Pinia + Persistent State**
Единое хранилище состояния приложения:
```typescript
// Клиенты (clientStore)
- Список всех клиентов
- CRUD операции (create, read, update, delete)
- API интеграция (с имитацией задержки)

// Фильтры (filterStore)
- Поисковой запрос
- Статус фильтр
- Автоматическое сохранение в localStorage
- Реализовано через `pinia-plugin-persistedstate`
```

### 3. **Form Validation с Vuelidate**
Валидация на уровне полей:
- `name`: обязательное, минимум 2 символа
- `email`: обязательное, корректный формат
- `phone`: обязательное, минимум 11 символов
- `status`: обязательное поле

Валидация работает в реальном времени с визуальной индикацией ошибок.

### 4. **Vue Router**
Структура маршрутов:
- `/clients` — список клиентов  
- `/clients/new` — создание нового клиента
- `/clients/:id/edit` — редактирование клиента

Навигация на основе состояния (после сохранения → автоматический редирект на список).

### 5. **UI Components: PrimeVue**
- `DataTable` — для отображения списка с сортировкой и пагинацией
- `InputText` — текстовые поля
- `Select` — выпадающие списки
- `Button` — кнопки с различными вариантами
- `Toast` — уведомления об ошибках/успехе

Оформление через **Tailwind CSS** + **PrimeUI theme**.

### 6. **TypeScript Strict Mode**
Весь код полностью типизирован для безопасности:
- Явные типы для всех функций и переменных
- Исключает категорию ошибок на этапе разработки

## 📋 Функциональность

### Список клиентов (`/clients`)
- ✅ Таблица с сортировкой и пагинацией
- ✅ Поиск по имени и email
- ✅ Фильтр по статусу (new, active, blocked)
- ✅ Состояния загрузки
- ✅ Кнопки: Создать, Редактировать, Удалить

### Форма клиента (`/clients/new` и `/clients/:id/edit`)
- ✅ Валидация всех полей
- ✅ Визуальная индикация ошибок
- ✅ Кнопки Сохранить/Отмена
- ✅ Редирект после сохранения

## 🔧 Что было бы улучшено при большем времени

### 1. **API Integration**
- Реальное подключение к REST или GraphQL API
- Обработка сетевых ошибок с retry-логикой
- Кэширование данных (например, через `vue-query`)

### 2. **Тестирование**
- Unit тесты (Vue Test Utils + Vitest)
- E2E тесты (Playwright или Cypress)
- Покрытие: валидация формы, CRUD, фильтрация, навигация

### 3. **UX/UI**
- Доступность (WCAG + contrast + reduce motion)
- Адаптивный дизайн для мобильных

### 4. **Code Quality**
- Pre-commit hooks (husky + lint-staged)
- Автоматический форматирование (Prettier или Biome)
- Расширенные ESLint правила


## 📁 Структура проекта

```
src/
├── app/              # Точка входа приложения
├── pages/            # Страницы (layout уровень)
│   ├── client-create/
│   ├── client-edit/
│   └── clients-list/
├── features/         # Функциональные модули
│   └── client-form/
│   └── clients-filter/
│   └── clients-list/
│   └── client-delete/
├── entities/         # Бизнес сущности
│   └── client/
│       ├── model/    # Store и логика
│       └── types/    # Типы
├── shared/           # Общий код
│   ├── api/
│   └── data/
```
